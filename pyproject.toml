[project]
name = "rrt-motion-planner"
version = "0.1.0"
description = "Sampling-based motion planning with RRT, RRT star, and PRM, including a comparative benchmark harness."
readme = "README.md"
requires-python = ">=3.12"
license = { file = "LICENSE" }
authors = [{ name = "Emre Yilmaz" }]
keywords = ["robotics", "motion-planning", "rrt", "path-planning", "python"]
dependencies = ["numpy>=2.0", "scipy>=1.14", "matplotlib>=3.9"]

[dependency-groups]
dev = ["pytest>=8.3", "ruff>=0.8", "mypy>=1.13"]

[project.optional-dependencies]
dev = ["pytest>=8.3", "ruff>=0.8", "mypy>=1.13"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/rrt_planner"]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = ["slow: excluded from the default run"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "UP", "B", "SIM", "RUF"]

[tool.mypy]
python_version = "3.12"
strict = true
files = ["src/rrt_planner"]

# SciPy ships no type information for scipy.spatial, so the k-d tree is untyped at the
# boundary. Every value crossing that boundary is converted explicitly before use.
[[tool.mypy.overrides]]
module = ["scipy.*"]
ignore_missing_imports = true
