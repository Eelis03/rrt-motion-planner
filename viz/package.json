{
  "name": "rrt-planner-viz",
  "version": "0.1.0",
  "private": true,
  "description": "Canvas animation of planner tree growth, driven by JSON traces exported from rrt_planner.",
  "license": "MIT",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "serve": "python -m http.server 8000"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
